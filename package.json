{
  "name": "ships",
  "version": "1.0.0",
  "description": "Dolgozat5",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx lite-server",
    "api": "hai-server --watch database.json --port 8000"
  },
  "keywords": [],
  "author": "RP",
  "license": "ISC",
  "devDependencies": {
    "hai-server": "^0.0.4",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
